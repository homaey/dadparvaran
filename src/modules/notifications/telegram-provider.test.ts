import { afterEach,describe,expect,it,vi } from "vitest";import { TelegramBotApiProvider } from "./telegram-provider";
afterEach(()=>vi.unstubAllGlobals());describe("Telegram provider",()=>{it("uses Bot API sendMessage with JSON",async()=>{const fetchMock=vi.fn().mockResolvedValue({ok:true,json:async()=>({ok:true})});vi.stubGlobal("fetch",fetchMock);await new TelegramBotApiProvider("secret").send({telegramId:"123",title:"هشدار",message:"مهلت گذشته است"});expect(fetchMock).toHaveBeenCalledWith("https://api.telegram.org/botsecret/sendMessage",expect.objectContaining({method:"POST"}));const body=JSON.parse(fetchMock.mock.calls[0][1].body);expect(body).toEqual({chat_id:"123",text:"هشدار\n\nمهلت گذشته است"})});it("surfaces Telegram errors",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:false,status:400,json:async()=>({ok:false,description:"chat not found"})}));await expect(new TelegramBotApiProvider("secret").send({telegramId:"x",title:"t",message:"m"})).rejects.toThrow("chat not found")})});
